name: Schedule Test
#test
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show trigger
        run: |
          echo "Trigger: ${{ github.event_name }}"
          date -u
